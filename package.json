{
  "name": "pocketcode-app",
  "private": true,
  "version": "0.1.0",
  "description": "Control Claude Code from your phone — MVP (relay + CLI + web viewer).",
  "scripts": {
    "relay": "node relay/server.js",
    "host": "node cli/pocketcode.js"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.0"
  }
}
